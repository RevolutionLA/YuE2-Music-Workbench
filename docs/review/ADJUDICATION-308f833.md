# ADJUDICATION-308f833 — 中立裁定报告（第三轮：蓝军 G3/G2b/G4 × RESPONSE 308f833 × 第三方 N-1…N-13）

裁定人：中立裁定员（全新角色，未参与前三任何一轮；不采信任何一方自述，只认本人到代码复核的证据）。
基线：commit `308f833` + 工作区未提交改动（`git status` 实测 15 改 6 增，含 `tests/`、`LICENSE`）。
裁定前本人实读/实测：`app.py`（守卫 :134-254、批量队列 :1919-2280、`import main` :44/:58）、`dsh-plugin/lib/client.js`（:300-380 + `git show HEAD:` 基线对照）、`tests/test_review_fixes.py`、三份蓝军报告、RESPONSE（含第三轮追加）、第三方审计。实跑：`py312/python.exe -m unittest discover -s tests` → **Ran 47 tests … OK**；对 `main.cp312-win_amd64.pyd` 做只读二进制符号抽取。**未重启任何进程、未发任何写请求、未跑任何 .bat。**

⚠️ 取证时效声明：`app.py` 在裁定过程中被再次修改（mtime 2026-09-26 21:51:46，批量区行号整体 +4：先后两次读取同一区域行号不一致，本人以 21:51 后的行号为准并逐处复验）。本报告行号只对当前工作区快照有效（这本身就是 D15 的老毛病，见第三节）。

## 一、争议条目的裁定表

只裁三方有分歧或流程需要记账的条目；48 条双方无争议的"已修"不重审。

| 编号 | 争点 | 裁定 | 关键证据（本人实核） | 一句话处置 |
|---|---|---|---|---|
| N-1 | B21 favicon 角标：整改方称已修 → 第三方判"100% 失效"（XHR 无 `.ok`）→ 整改方二次修正 | **第三方当时判定成立；当前代码正确；维持 RESPONSE 八-1 的自纠** | 基线 `git show HEAD:client.js:332` 即 `x1.ok`（同步 XHR 时代角标就从未亮过）；一轮整改照抄同款错误；**现码 `client.js:334-346` 用 `x.status >= 200 && x.status < 300`（:341），`:364` 有 clearInterval+observer 清理**，`node --check` 级结构正确 | 维持整改方二次修法；原始"已修+列进放心条目"是夸大，已由八-1 记账 |
| N-3 | Y1"LAN 已修"措辞 vs DNS 重绑定异议；现码是否闭环 | **原措辞夸大成立（维持第三方）；现码已闭环（采纳整改方换根修法）** | 一轮原文"放宽为只认同源"（RESPONSE:122）用的正是被驳回的 Origin==Host 判据。现码：主机名白名单 `_lan_host_allowlist`（app.py:156-168）、通配绑定无名单**拒启动**（:185-191）、写请求要求 Origin 主机名命中白名单**且端口等于网关端口**（`_origin_in_allowlist` :216-227）、LAN 模式下 Host 先过白名单（:233-235）——evil.com 重绑定到局域网 IP 时 `Host: evil.com` 不在名单，403 在 Origin 比对之前 | 残余两点如鲠在喉但不构成重开：`u.port in (None,_P_GW)`（:227）把 scheme 默认端口（80/443）也算同源；白名单退路是绑定地址本身。LAN 启用前写进发布门槛 |
| N-2 | worker 过期快照整体回写吞追加；状态文件非原子写 | **第三方成立；当前已修，维持整改** | `_batch_write` tmp+`os.replace`（app.py:1919-1925）；worker 锁内重读、只改自己一格（:1978-1986 登记、:2012-2023 取消终态、:2027-2036 完成回写）；用例 `test_write_is_atomic_and_leaves_no_temp`（tests:180-187）、`test_store_only_replaces_listed_ids`（tests:189 起）实跑绿 | 修实、测实 |
| N-2b | HTTP 处理函数仍走 `_batch_snapshot→改→_batch_store` 整份回写 | **可接受残余（本地单用户前提），不是活跃缺陷——但判据要钉死** | `_batch_store` docstring 明写窗口与收敛条件（app.py:1936-1943）；调用点 batch_stop（:2210/:2217）、batch_status 两段自愈（:2169/:2182）、resume/retry（:2222/:2254 系）；毫秒级窗口 × 单用户点击频率，且 worker 侧已免疫（锁内重读） | **裁定：接受，但"局域网多人用之前必须收敛成锁内最小写"从散文升级为硬约束**——与 Y2 同类，做进 LAN 开关联检 |
| N-4 | 单首在跑时批量条目抢注 current job | **第三方成立；当前已修** | worker 改为 `with _GPU_SEM`（:1972）排队**之后**才 `_gen_set_job`+写 meta（:2009-2010 区段，锁内），注释点名 N-4 | 修实；无自动化（要真计算），结构可 grep |
| N-5 | 取消后条目卡 running，被自愈改写成"服务重启"假文案 | **第三方成立；当前已修** | 取消路径落终态 `cancelled`+"已由用户取消" 并清 current（:2012-2023）；"服务重启，任务中断"只落在真非 running 的残留分支（:2173-2182） | 修实 |
| N-6 | `_batch_ensure_worker` 无锁判活可起双 worker | **第三方成立；当前已修** | `_BATCH_WORKER_LOCK` 判活+赋值同临界区（:2039-2047，锁定义 :1929） | 修实 |
| G4-D14 | `audiocpp.py` 死代码说 | **蓝军编造坐实；维持整改方翻案，且翻案本身也成立** | 本人对 `main.cp312-win_amd64.pyd` 二进制取证：独立模块名字符串 `\x00audiocpp\x00` 紧邻 `HTTPException`（导入符号区）命中 1 次，且 `resolve_model_id` 全仓唯一定义在根 `audiocpp.py:16`——**内核确实 live-import 它，删了必崩**（app.py 自身不 import，G4 的 grep 视角够不到 pyd） | 编造一条 = G4 全篇措辞降权处理（RESPONSE 四-2 的处置正当）；顺带核：`chunking` 在 pyd 与全部 .py 均 0 命中，G4 对 `src/chunking.py` 的"0 引用"属实，整改方"保留待证"是诚实的保守 |
| 任务-5 | "main.pyd 缺失则整仓跑不起来"是否伪前提 | **对网关为真、对"整仓"为夸大——非伪前提但需精确化** | app.py:44 `import main`、:58 `app = main.app` 无条件——缺 pyd 网关起不来；但 tests/test_review_fixes.py:24-32 在 import app 前注入 `sys.modules["main"]` 替身（app 只用 `main.app`/`main.settings` 两处，注释如实），47 例即在不依赖 pyd 语义下通过 | 结论：该说法作为"不能拿测试证明内核行为"的限定成立；作为"仓库不可用"的断言过宽 |
| 任务-5b | G1/G2 阵亡留下的覆盖缺口 | **维持三方共认，流程自记账** | RESPONSE 四-1 主动交代两子代理 150 轮静默死亡；第三方二/三节独立发现 N-2/4/5/6 与 八-2 两条**全部落在蓝军射程外**（第三方四节自认"漏网=三块都在 G1 阵亡带走的部分"）；队列/取消/并发一角本轮唯一系统审查者是第三方 | 生成主链路之外的 LRC 对齐（`src/lrc_align.py`）与 RVC 主链**连续两轮零对抗覆盖**，写死进下一轮必做范围 |
| N-12 | `_CANCEL_EVENT` 全局单例、多处无条件 `clear()` | **承认定性；"记账暂不修"裁定为诚实推迟（本地单用户），但条件要焊死** | 现码 clear 点位：worker 起线程（:1956）、batch_status 自愈（:2171）、resume/retry（:2247/:2273）、启动自愈（:3844）——竞态窗口与后果（连发剩余条目复活）与第三方描述一致，非静默丢数据 | 与 LAN/多人开放做硬关联：一旦 `YUE2_ALLOW_LAN`，此项与 N-2b、Y2 同批必须先修 |
| N-13 | 崩溃自愈无失败计数、每轮复活 | **代码已修；RESPONSE 八-2 的"记账暂不修"已过期（反向失实：低报）** | `_REVIVE_WINDOW_SEC=600`/`_REVIVE_MAX=3`（app.py:1930-1933），自愈路径复活计数超限即停 error 等人工（:2154-2163）——正是第三方建议的形状 | 裁"整改方以行动赢过自己的报告"；八-2 该行须在提交前改口径，否则报告与树互相证伪 |
| 八-3 | 整改方反驳第三方"GET /abc/analyze 无长度闸门" | **维持整改方；第三方该条失实** | 两入口均 `abc[:200_000]`（app.py:811、:818），为提交上限 20000 的 10 倍；第三方引 app.py:759 在现树对应不到无闸门代码 | 记入第三方账目：其"诚实清单"整体可信，此条例外 |
| 夸大双裁 | Y1 措辞 + B21"结构可 grep 验证" | **两项均维持第三方（夸大成立）** | Y1 见上；B21 一轮 RESPONSE:107 称"已修+可验证"，而修复本身失效（N-1），八-1 已自纠入账 | 属报告层缺陷，不阻断代码；提交前 RESPONSE 保持现状（已含自纠）即可 |
| 测试诚实性 | "Ran 47 tests OK" + 两处弱用例 | **维持整改方：数字真实、用例已加固** | 本人实跑 47 例 OK（1.15s）；`test_private_default_path_is_env_overridable` 现真调 `app._gsv_root()` 断言 env 生效（tests:460-474，注释明说旧版只 grep 源码）；`test_lan_mode_allows_only_whitelisted_host_and_port`（tests:258-272）覆盖异端口 403 与 evil Origin 403 | 无谎报 |

## 二、时序与共同前提审查（谁在哪一刻是对的）

1. **N-1 三方都对，只是时点不同**：基线（`x1.ok`，同步 XHR）角标从未亮过 → 一轮整改"异步化了但仍不亮"（第三方复测属实）→ 现树 `x.status` 判据正确。文档链条（RESPONSE 一轮:107 夸大 / 八-1 自纠）与代码链条完全对得上。
2. **N-3 同理**：一轮判据（Origin 主机==Host 主机）在 LAN 下对重绑定天然失守，第三方的攻击推演成立；现判据（自配白名单+端口+拒启动）把"两个都由攻击者域名决定的字符串互比"换成了服务端持有的名单比对——**换根正确**。注意整改方引用的修法正是第三方"修法"栏的形状，属采纳而非翻案。
3. **批量区在裁定期间仍在改动**（行号 +4 漂移；八-2 与现树的 N-13 矛盾很可能就是"报告先行、代码后补"或"报告滞后"造成）。裁定纪律：**一切后端结论以 21:51 快照为准，发布前需按第四节清单重跑一次冒烟**。
4. **最大的共同前提，三方均已声明、本庭重申**：`reload=False`，在跑网关仍是旧代码。**app.py 的一切修复（守卫、原子写、N-2/4/5/6、S4/S11/Y1/Y5/S7/S8、D22……）当前在运行时全部不生效**；前端 index.html 与 client.js 即时生效——组合态即 D5。任何"已安全"的宣传在重启前都不成立；重启须由用户授权（有 CPU 生成在跑），重启后第一批冒烟项 = N-2/N-4/N-5 真实队列行为（第三方五-5 的原话，本庭背书）。
5. **未验证欠账继承**：S1/S2/S3 端到端、.bat 全链、LAN 实机——一、二轮声明继续有效，本轮无新增验证。

## 三、缺陷分箱（结论前置的记账）

- **(a) 代码缺陷（当前树仍活着的）**：无 🔴 级新发现。存活残量：① `_batch_store` HTTP 整写窗口（裁定为可接受残余，LAN 前必须收敛）；② `_origin_in_allowlist` 端口判据含 scheme 默认端口（LAN 前收紧）；③ `_CANCEL_EVENT` 单例竞态（已记账，LAN 前必拆）；④ Y5 `?force=1` 串行锤起子进程（声明过的残余）；⑤ N-9 之 .bat token 日志 append/truncate 语义不一致（第三方原条的第二半，本轮未见整改入账，**追认为待办**）。
- **(b) 主张/报告缺陷**：① G4-D14 编造（已定案：内核二进制反证）；② RESPONSE 一轮 Y1/B21 两处夸大（八-1 已自纠）；③ **RESPONSE 八-2 N-13 行与现树相反（代码已修、报告称未修）——提交前必须改**，否则第一份公开 commit 里报告与代码互斥；④ 第三方"abc/analyze 无闸门"一条失实（八-3 维持反驳）；⑤ 全部历史报告行号继续漂移（app.py 本轮编辑实证），D15 的"以函数名/路由定位"处置维持但未被本轮报告自身遵守。
- **(c) 已记档可接受残余（本地单用户前提下）**：GET 无 Origin 时可本机读取（S3 残余）、Y2 stderr 原文、B16/B18/B22/B25 前端机械项、S12/D18 哈希锁（公开分发前闭环，整改方自己列了）、Y8 gtcrn 出处（LICENSE 已如实写"发布前核对"）。

## 四、结论（单条裁定）

**结论：本工作树以"本地自用 + 报告同步修订 + 一次受控重启冒烟"为前置条件，可以发布为该仓库的首个公开 commit；在满足下列门槛之前不得推送宣传"已安全/已修"。**
阻断门槛（全部是 (b) 类主张修正与流程项，无新增代码级 P0）：
1. RESPONSE 八-2 N-13 行改为"已修（_REVIVE_MAX=3，app.py:1930-1933/:2154-2163）"，并复核提交时点的行号；
2. D16 版本策略（tag/CHANGELOG/PROMOTION 版本号措辞）需用户拍板——这是唯一悬置的用户决策；
3. README 中英版必须把"重启网关才生效 + 重启中断在跑一首"钉在显眼处（一轮已写入，维持）；发布说明附"重启后第一批冒烟：追加排队/取消/重启自愈"三条；
4. S12/D18/Y8（哈希锁、gtcrn 出处、LICENSE 闭环）按整改方自列的"公开分发前必须闭环"执行——接受作为公开后的第一优先级，而非接受其缺席作为发布谎言（LICENSE 已如实标注）。
不阻断：N-2b/N-12/端口默认值/`force=1`——全部是本地单用户下已声明、已记账的残余；一旦 `YUE2_ALLOW_LAN` 或多人使用意图出现，(a)①②③ 立即升为硬门槛。

**裁定摘要行**：N-1 维持第三方（现码已修）；N-3 原措辞夸大维持、换根修法采纳；N-2/4/5/6 全部修实、HTTP 整写判可接受残余；G4-D14 编造定案、翻案维持；八-3 维持整改方；八-2 N-13 判报告滞后（代码已修）；47 测试复跑属实；发布可行，但生效与诚实性以一次授权重启和三处报告修订为代价。
